extends main

include ../mixins/user-sidebar
include ../mixins/header

block vars
  - var collapseSidebar = false

block content
  +header
  .d-flex.user-dashboard
    .sidebar__column
      +user-sidebar(collapseSidebar)
    .content
      .container
        .row
          .col-12
            h1 
              block title
        block primary
    .right-content
      block right